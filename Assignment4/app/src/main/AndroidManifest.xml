<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pizzaapp.yummypizzas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreenActivity"></activity>
        <activity android:name=".EmployeeOrderTrackerActivity" />
        <activity android:name=".CustomerOrderSummaryActivity" />
        <activity android:name=".CustomerInfoActivity" />
        <activity android:name=".CustomerPizzaOrderActivity" />
        <activity android:name=".CustomerScreenActivity" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>